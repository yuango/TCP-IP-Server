**分配动态内存，每次都不同
**exit(int)结束整个进程且返回int
**_exit()/_EXIT()立即结束所有东西，不再执行清理程序，一般不用，只有内核会调用
**system(char *cmd)可以在程序中运行命令行
**用ctl+c可以结束多个进程(但是这多个进程必须是一个组的，即父子进程)
**本机实验时发现有时子进程没有动作时，父进程已经结束了，那此时子进程的父进程就变了(变成初始化进程1)，此时我们可以直接写命令，因为我们的命令也是运行在初始化进程下的
**孤儿进程都会被初始化进程收养
**第七个文件是使用wait来结束子进程，防止僵尸进程的发生
**用kill来使第8个程序执行非正常结束
**每个子进程都会把它的结束信息带给父进程
